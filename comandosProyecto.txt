docker images
docker ps
docker ps -a
docker run -d --name (nombre del contenedor) -p (puerto) (nombre de la imagen)
docker images


docker compose up --bu
docker compose down -v --rmi allild // para eliminar


mvnw.cmd clean package -DskipTests
docker compose down
docker rm -f (imagen)
docker-compose build --no-cache
docker-compose up

docker compose stop frontend
docker rm -f ropa-frontend
docker compose build --no-cache frontend
docker compose up -d frontend

docker exec -it ropa_mariadb mariadb -u root -p

luul xgdm zcer aode

////////////////////////////////////////////////////////////////

DELETE pi
FROM producto_imagenes pi
JOIN productos p ON pi.producto_id = p.id
JOIN tiendas t ON p.tienda_id = t.id
WHERE t.nombre = 'PullAndBear';

DELETE pts
FROM producto_talla_stock pts
JOIN productos p ON pts.producto_id = p.id
JOIN tiendas t ON p.tienda_id = t.id
WHERE t.nombre = 'PullAndBear';

DELETE f
FROM favoritos f
JOIN productos p ON f.producto_id = p.id
JOIN tiendas t ON p.tienda_id = t.id
WHERE t.nombre = 'PullAndBear';

DELETE ic
FROM items_carrito ic
JOIN productos p ON ic.producto_id = p.id
JOIN tiendas t ON p.tienda_id = t.id
WHERE t.nombre = 'PullAndBear';

DELETE p
FROM productos p
JOIN tiendas t ON p.tienda_id = t.id
WHERE t.nombre = 'PullAndBear';

/////////////////////////////////////////////



































































